import { describe, it, expect } from "vitest";
// import { ... } from '../src';

describe("calendar-core basic", () => {
  it("should have a working test setup", () => {
    expect(1 + 1).toBe(2);
  });
});
